<%= form_with(model: point, local: true) do |form| %>
  <% if point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(point.errors.count, "error") %> prohibited this point from being saved:</h2>

      <ul>
      <% point.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hour %>
    <%= form.datetime_select :hour, id: :point_hour %>
  </div>

  <div class="field">
    <%= form.label :flag %>
    <%= form.text_field :flag, id: :point_flag %>
  </div>

  <div class="field">
    <%= form.label :bank_id %>
    <%= form.text_field :bank_id, id: :point_bank_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
